请在当前原始MeMR项目上增加“元数据质量与可用性鲁棒性实验”，并实际准备四组可并行运行的训练任务。严格遵守以下要求。

# 一、核心约束

1. 不得修改、覆盖、删除或重命名任何原始项目文件。
2. 原始项目已有的训练逻辑、模型结构、数据划分、任务顺序、超参数和随机种子必须保持不变。
3. 只能使用编号为2，3，4，5的卡。
4. 最后给出一个执行的命令即可，不需要你来执行。
5. 只允许在项目根目录下新增一个独立目录，例如：


```text
metadata_robustness_experiments/
```

4. 优先通过以下方式实现实验：

   * 外部配置覆盖；
   * 独立训练包装器；
   * import后进行运行时注入；
   * monkey patch；
   * 独立元数据预处理文件。

5. 如果确实无法在不修改原代码的情况下替换元数据，则：

   * 为每个实验在 `metadata_robustness_experiments/workdirs/` 下创建原项目的独立副本；
   * 只修改副本；
   * 不得修改原始项目；
   * 副本复制时排除 `.git`、历史输出、缓存和大型无关文件。

6. 在执行任何操作前，生成原始受版本控制文件的SHA256清单。

7. 完成全部修改后再次检查，确保原始已有文件的哈希值没有发生变化。

8. 所有新增代码必须兼容原始完整元数据设置。新增实验功能关闭时，运行结果必须与原始项目一致。

9. 不要在训练过程中运行验证集或测试集评估。

10. 不允许因关闭评估而改变训练轮数、总步数、学习率调度、梯度累积、保存频率或其他训练设置。

11. 四组实验各使用一张GPU，GPU编号依次为0、1、2、3，不使用DDP，不让单个实验占用多张卡。

12. 四组实验除元数据设置、输出目录、运行名称和GPU编号外，其他参数必须完全相同。

13. 训练随机种子必须继承原始项目的设置。额外设置独立的元数据扰动随机种子：

```text
metadata_seed = 20260627
```

并使其可通过命令行参数修改。

# 二、首先检查原始项目

请先自动检查并确认以下内容：

1. 原始训练入口文件和实际训练命令。
2. 原始配置文件及参数读取方式。
3. 元数据的原始存储格式和加载位置。
4. 元数据是在文本级、字段级还是向量级加载。
5. 医学知识库元数据包含哪些字段，例如：

   * task name；
   * department name；
   * description；
   * keywords；
   * symptoms；
   * aliases；
   * 其他字段。
6. 元数据编码器及元数据向量生成位置。
7. MDTM中的以下变量对应到代码中的具体实现：

   * 静态元数据表示；
   * 可训练任务表示；
   * 动态融合系数；
   * 动态任务表示；
   * 最终任务匹配权重。
8. 模型保存逻辑是否能够完整保存：

   * LoRA参数；
   * 当前任务模块；
   * 历史任务模块；
   * MDTM参数；
   * ATR参数；
   * 可训练任务向量；
   * 元数据相关参数；
   * 优化器；
   * 学习率调度器；
   * AMP scaler；
   * trainer state；
   * 随机数状态。
9. 原始项目是否使用WandB、TensorBoard或其他实验记录工具。
10. 原始训练是否包含自动评估逻辑，并找到安全关闭评估但不改变训练过程的方法。

检查完毕后，在新目录下生成：

```text
metadata_robustness_experiments/project_inspection.md
```

其中写明：

* 找到的训练入口；
* 原始训练命令；
* 元数据加载位置；
* 模型保存方式；
* 本次采用的非侵入式实现方案；
* 四组实验与原始代码之间的调用关系。

不要只给出分析，请继续完成代码修改和运行脚本。

# 三、实现统一的元数据扰动模块

新增一个独立模块，例如：

```text
metadata_robustness_experiments/metadata_perturbation.py
```

提供统一接口：

```python
perturb_metadata(
    metadata,
    mode,
    metadata_seed,
    metadata_encoder=None,
)
```

至少支持以下模式：

```text
full
missing_50
noisy_30
stale_coarse
institution_mix_40
none
```

本次实际训练只运行：

```text
missing_50
noisy_30
stale_coarse
institution_mix_40
```

`full`和`none`用于兼容已有实验，不重新运行，除非原项目明确需要。

元数据扰动必须优先发生在“原始文本或结构化字段编码为向量之前”，不要优先采用对向量直接添加高斯噪声的方法。

如果原始项目只保存了元数据向量，没有可访问的原始元数据文本，则可以实现向量级后备方案，但必须：

1. 在日志中明确记录使用了后备方案；
2. 在说明文档中解释具体操作；
3. 不得悄悄改变实验定义。

# 四、四组实验的精确定义

## 实验1：missing_50

目的：模拟元数据不完整。

操作规则：

1. 科室名、任务名或任务ID始终保留，不允许删除。
2. 对description、keywords、symptoms、aliases等非标识字段，随机删除50%的有效信息单元。
3. 对列表字段：

   * 随机保留50%的条目；
   * 使用向上取整；
   * 每个原本非空的字段至少保留1条。
4. 对文本描述字段：

   * 按中文和英文句号、分号、逗号或换行切分为语义片段；
   * 随机保留50%的片段；
   * 至少保留1个非空片段。
5. 对每个任务分别执行扰动，不能出现某个任务元数据完全为空。
6. 所有随机操作使用 `metadata_seed`，确保完全可复现。

实验名称：

```text
missing_50
```

## 实验2：noisy_30

目的：模拟元数据中的噪声、错误标注和无关信息。

操作规则：

1. 科室名、任务名和任务ID保持不变。
2. 对每个任务的description、keywords、symptoms和aliases等内容单元，随机选取30%进行替换。
3. 替换内容必须来自其他任务的对应字段，不能来自当前任务。
4. 原始内容单元数量应基本保持不变。
5. 替换时优先保持字段类型一致：

   * symptom替换为其他任务的symptom；
   * keyword替换为其他任务的keyword；
   * description片段替换为其他任务的description片段。
6. 如果对应字段无法找到替换项，则从其他任务的所有非标识元数据中采样。
7. 不允许使用测试集文本构造噪声。
8. 所有随机操作使用 `metadata_seed`。

实验名称：

```text
noisy_30
```

## 实验3：stale_coarse

目的：模拟长期未更新、只保留旧版粗粒度知识的元数据。

操作规则：

1. 首先检查项目或医学知识库中是否存在：

   * metadata version；
   * timestamp；
   * historical metadata；
   * old metadata；
   * legacy metadata。
2. 如果存在真实历史版本：

   * 对所有任务统一使用最早可用版本；
   * 保存所选版本编号和时间信息。
3. 如果不存在历史版本，则使用“模拟过时的粗粒度元数据”：

   * 保留任务名、科室名；
   * 保留description中的第一条或最概括的一条描述；
   * 删除全部或绝大多数细粒度symptoms、keywords和aliases；
   * 删除后续由大模型扩展得到的详细说明；
   * 不使用其他任务的错误信息。
4. 该设置的本质是：

   * 元数据仍然正确；
   * 但信息粒度粗；
   * 缺少新的、细粒度的任务特征。
5. 输出文件和日志中必须将该设置明确标记为：

```text
simulated_stale_coarse_metadata
```

若没有真实历史版本，不得声称使用了真实过时知识库。

实验名称：

```text
stale_coarse
```

## 实验4：institution_mix_40

目的：模拟不同医疗机构之间科室分类、术语体系和任务边界不一致。

操作规则：

1. 科室名、任务名和任务ID保持当前机构的定义不变。
2. 首先基于完整元数据确定每个任务最相近的另一个任务：

   * 优先使用原项目的元数据编码器生成完整元数据向量；
   * 计算任务之间的余弦相似度；
   * 排除任务自身；
   * 选择相似度最高的其他任务作为“相邻机构映射任务”；
   * 相似度相同时按任务ID排序，确保结果确定。
3. 不得使用测试集标签或测试集回答确定相邻任务。
4. 将当前任务40%的非标识元数据内容替换为相邻任务对应字段的内容。
5. 保留60%的本任务元数据。
6. 保持元数据总体长度基本不变。
7. 字段对应替换规则与 `noisy_30` 相同。
8. 与随机噪声不同，该实验中的替换来源必须固定为语义最相近任务，用来模拟系统性的科室边界重叠和机构本体不一致。
9. 保存每个任务的相邻任务映射关系和原始相似度。

实验名称：

```text
institution_mix_40
```

# 五、元数据完整记录

每个实验在训练开始前必须保存：

```text
metadata/original_metadata.json
metadata/perturbed_metadata.json
metadata/metadata_stats.json
metadata/metadata_stats_by_task.csv
metadata/task_mapping.json
```

其中：

## metadata_stats.json至少包含

```json
{
  "condition": "",
  "metadata_seed": 20260627,
  "total_tasks": 0,
  "original_total_units": 0,
  "perturbed_total_units": 0,
  "retained_units": 0,
  "removed_units": 0,
  "replaced_units": 0,
  "retention_ratio": 0.0,
  "removal_ratio": 0.0,
  "replacement_ratio": 0.0,
  "mean_original_perturbed_cosine": 0.0,
  "min_original_perturbed_cosine": 0.0,
  "max_original_perturbed_cosine": 0.0
}
```

如果可以调用原始元数据编码器，则在训练前计算每个任务：

```text
cosine(original_metadata_embedding, perturbed_metadata_embedding)
```

这只是元数据扰动诊断，不属于模型验证集或测试集评估，可以执行。

## metadata_stats_by_task.csv至少包含

```text
condition
task_id
task_name
original_units
perturbed_units
retained_units
removed_units
replaced_units
retention_ratio
replacement_ratio
original_perturbed_cosine
source_task_for_replacement
```

# 六、四组训练配置

为四个实验分别生成独立配置文件，例如：

```text
configs/missing_50.yaml
configs/noisy_30.yaml
configs/stale_coarse.yaml
configs/institution_mix_40.yaml
```

配置文件必须从原始基线配置自动继承，并仅覆盖以下内容：

```text
metadata_mode
metadata_seed
output_dir
run_name
evaluation_strategy/no_eval相关设置
```

除上述内容外，不得手工重写其他训练参数。

请生成一个参数对比文件：

```text
resolved_configs/config_comparison.csv
```

每行对应一个参数，每列对应一个实验。程序必须自动检查四组配置，确保除了以下参数外完全一致：

* metadata_mode；
* output_dir；
* run_name；
* GPU相关环境变量；
* 日志文件路径。

如果发现学习率、epoch、batch size、gradient accumulation、LoRA配置、任务顺序、数据集、温度系数、ATR参数、损失权重、随机种子等存在差异，则立即终止运行并报错。

# 七、关闭训练过程中的评估

四组训练均不在训练过程中执行验证或测试。

请根据原项目框架安全设置，例如：

```text
evaluation_strategy = "no"
do_eval = false
load_best_model_at_end = false
```

但必须注意：

1. 不得因此改变总训练步数。
2. 不得改变学习率调度器的总步数。
3. 不得改变原始checkpoint保存间隔。
4. 不得将“保存最佳模型”错误替换为“不保存模型”。
5. 因为没有验证指标，不要使用best checkpoint概念。
6. 每个实验必须保存最终checkpoint。
7. 如果原项目原本按epoch或step保存中间checkpoint，则继续保持相同策略。
8. 不要运行任何LLM评分、任务匹配准确率、FWT、FR、BWT或测试集生成。

可以额外生成一个独立的后续评估脚本：

```text
evaluate_saved_checkpoints.sh
```

但本次不要执行该脚本。

# 八、模型与训练状态保存要求

每组实验的输出目录结构统一为：

```text
outputs/metadata_robustness/<condition>/
├── checkpoints/
├── final_model/
├── trainable_state/
├── metadata/
├── configs/
├── logs/
├── environment/
├── metrics/
└── manifest/
```

每组实验必须保存：

1. 最终模型checkpoint。
2. 原项目规定的中间checkpoint。
3. LoRA adapter参数。
4. MDTM全部参数。
5. ATR全部参数。
6. 所有可训练任务表示。
7. 当前任务的task-specific module。
8. 训练过程中涉及的其他可训练模块。
9. optimizer state。
10. scheduler state。
11. gradient scaler state，如果使用混合精度。
12. trainer state。
13. Python、NumPy、PyTorch和CUDA随机数状态。
14. 原始基础模型的名称和绝对路径。
15. tokenizer名称、路径和配置。
16. 数据集路径和数据集指纹。
17. 完整解析后的训练配置。
18. 实际执行的命令。
19. Git commit hash。
20. 原始项目文件哈希清单。
21. Python包环境。
22. CUDA、PyTorch、transformers、peft等版本。
23. GPU型号和显存。
24. 元数据扰动前后文件。
25. 训练日志。

如果原项目使用参数高效微调，标准 `save_pretrained()` 不能覆盖自定义MDTM、ATR或任务向量时，必须额外保存：

```python
torch.save(custom_trainable_state_dict, ...)
```

并生成：

```text
trainable_state/state_dict_keys.txt
```

列出保存的所有参数名、shape、dtype和requires_grad状态。

同时执行完整性检查：

* 列出训练开始前所有 `requires_grad=True` 的参数；
* 列出最终checkpoint中已保存的参数；
* 确认所有可训练参数均已保存；
* 若有缺失，训练任务视为失败。

若完整7B基础模型重复保存会导致磁盘空间不可接受，则可以：

1. 保存基础模型标识和不可变文件哈希；
2. 保存LoRA adapter；
3. 保存全部自定义模块参数；
4. 保存所有任务向量；
5. 保存完整训练状态；
6. 提供一键恢复脚本。

但必须保证仅凭基础模型和实验目录即可完整恢复模型。

# 九、需要记录的关键训练数据

每组实验生成：

```text
metrics/train_metrics.jsonl
metrics/train_summary.json
```

`train_metrics.jsonl`按原项目日志频率记录：

```text
global_step
epoch
train_loss
learning_rate
grad_norm
tokens_or_samples_seen
elapsed_time
gpu_memory_allocated
gpu_memory_reserved
```

`train_summary.json`至少包含：

```json
{
  "condition": "",
  "gpu_id": 0,
  "training_seed": 0,
  "metadata_seed": 20260627,
  "start_time": "",
  "end_time": "",
  "status": "success",
  "total_epochs": 0,
  "total_steps": 0,
  "final_train_loss": 0.0,
  "minimum_logged_train_loss": 0.0,
  "average_train_loss": 0.0,
  "runtime_seconds": 0,
  "peak_gpu_memory_mb": 0,
  "trainable_parameter_count": 0,
  "total_parameter_count": 0,
  "checkpoint_path": "",
  "final_model_path": "",
  "metadata_retention_ratio": 0.0,
  "metadata_replacement_ratio": 0.0,
  "mean_metadata_cosine": 0.0
}
```

本轮不需要生成验证或测试性能。

四组训练结束后汇总生成：

```text
outputs/metadata_robustness/all_runs_summary.csv
```

至少包含：

```text
condition
gpu_id
status
training_seed
metadata_seed
total_steps
final_train_loss
minimum_train_loss
runtime_seconds
peak_gpu_memory_mb
original_metadata_units
perturbed_metadata_units
retention_ratio
replacement_ratio
mean_metadata_cosine
final_checkpoint
```

# 十、四卡并行运行

生成脚本：

```text
run_4_metadata_experiments.sh
```

GPU对应关系固定为：

```text
GPU 0 -> missing_50
GPU 1 -> noisy_30
GPU 2 -> stale_coarse
GPU 3 -> institution_mix_40
```

每个实验必须通过类似方式限制GPU：

```bash
CUDA_VISIBLE_DEVICES=0 ...
CUDA_VISIBLE_DEVICES=1 ...
CUDA_VISIBLE_DEVICES=2 ...
CUDA_VISIBLE_DEVICES=3 ...
```

注意：当设置 `CUDA_VISIBLE_DEVICES=2` 时，进程内部通常只看到 `cuda:0`，不要错误地继续指定内部设备为 `cuda:2`。

四个任务必须：

1. 使用独立输出目录；
2. 使用独立日志；
3. 使用独立run name；
4. 使用独立缓存子目录，如果原项目存在并发写入风险；
5. 使用独立WandB run id，或者统一禁用WandB在线上传；
6. 不共享会被覆盖的临时文件；
7. 不共享同一个checkpoint目录；
8. 支持断点续训；
9. 默认输出目录已存在时拒绝覆盖；
10. 只有显式传入 `--resume` 时才从最新checkpoint恢复。

脚本同时生成：

```text
logs/missing_50.stdout.log
logs/missing_50.stderr.log
logs/noisy_30.stdout.log
logs/noisy_30.stderr.log
logs/stale_coarse.stdout.log
logs/stale_coarse.stderr.log
logs/institution_mix_40.stdout.log
logs/institution_mix_40.stderr.log
```

提供：

```text
monitor_4_runs.sh
```

用于显示：

* PID；
* GPU；
* 条件名称；
* 当前step；
* 当前loss；
* 最近checkpoint；
* GPU显存；
* 运行状态。

提供：

```text
stop_4_runs.sh
```

只终止本次四个实验，不影响其他进程。

# 十一、训练前检查

正式训练前必须依次执行：

1. 元数据扰动单元测试。
2. 四个模式的元数据生成测试。
3. 确认原始元数据文件未被覆盖。
4. 确认四种扰动结果不同。
5. 确认相同seed重复运行得到完全相同的元数据。
6. 确认不同seed能够得到不同的随机扰动结果。
7. 确认每个任务仍然存在有效元数据。
8. 确认四组解析后配置除允许字段外完全一致。
9. 使用极小数据或单个batch执行一次smoke test。
10. smoke test不得覆盖正式输出目录。
11. 检查所有可训练参数均可保存和重新加载。
12. 比较原始项目文件修改前后的SHA256，确认无已有文件被修改。

生成测试报告：

```text
metadata_robustness_experiments/preflight_report.md
```

只有全部检查通过后，才启动四组正式训练。

# 十二、后续评估脚本

虽然本次不执行评估，但请生成一个独立脚本，使后续可以对六种设置进行统一评估：

```text
full
none
missing_50
noisy_30
stale_coarse
institution_mix_40
```

后续评估至少支持输出：

1. 各科室Average LLM Score。
2. 总体Average LLM Score。
3. Top-1 Task Matching Accuracy：

   ```python
   predicted_task = argmax(W_k)
   ```
4. 各科室任务匹配准确率。
5. 任务匹配混淆矩阵。
6. 平均正确任务权重。
7. 错误路由比例。
8. 如现有代码支持，再输出FWT、FR和BWT。

该脚本本次只生成，不运行，不访问测试集。

# 十三、最终交付内容

完成后请给出：

1. 新增文件列表。
2. 原项目文件哈希检查结果。
3. 四种元数据设置的实现说明。
4. 四个实际训练命令。
5. 四卡启动命令。
6. 输出目录说明。
7. 训练参数一致性检查结果。
8. 模型参数保存完整性检查结果。
9. smoke test结果。
10. 四个训练进程的PID。
11. 若训练已完成，给出四组训练摘要和checkpoint路径。
12. 若训练仍在进行，给出监控命令。
13. 任何失败任务的明确错误原因，不得伪造成功结果。

不要改变原始项目中的实验结果、模型文件和配置文件。不要为了适配新实验而重构原始项目。所有功能必须被限制在新建的元数据鲁棒性实验目录或独立运行副本中。
