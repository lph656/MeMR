你现在位于 MeMR 论文项目的原始代码仓库中。请对项目中“LoRA 任务模块的加权组合机制”进行全面、严格的代码审计，为回复以下审稿意见收集全部事实依据。

审稿意见：

“The formulation and implementation of adapter composition require clarification. The manuscript states that task-specific modules are weighted and aggregated, but it does not formally define the module type. Since the implementation is based on LoRA, the authors should specify whether the weighted composition is applied to LoRA A matrices, B matrices, full adapter updates, adapter outputs, or another representation. They should specify rank, target projections, scaling coefficient, dropout, and whether all task modules have identical architectures. It is also unclear whether Equation (6) aggregates only old task modules or includes the current task module. The role of the composed module and the newly trained module must be precisely described in both training and inference. The gradient flow to old task vectors, old task modules, metadata embeddings, and routing parameters should be stated explicitly.”

## 一、执行原则

1. 不要修改任何训练、推理、模型、配置或数据代码。
2. 不要重新训练模型，也不要运行耗时的大规模推理。
3. 优先通过静态代码分析、配置文件、训练脚本和已有日志确认事实。
4. 只有在静态分析无法确定 `requires_grad` 或实际前向路径时，才允许进行轻量级 dry-run 或参数打印；不要启动正式训练。
5. 不要根据论文描述推测实现，所有结论必须以代码为准。
6. 无法确定的信息统一标记为 `TBD`，并说明缺少什么证据。
7. 不得泄露 API key、token、用户名或私密绝对路径。
8. 最终只新增以下文件：

   * `ADAPTER_COMPOSITION_AUDIT.md`
   * 可选：`adapter_composition_audit.json`

## 二、需要定位的核心代码

请全局搜索并追踪以下关键词及其调用关系：

```text
LoRA
lora
LoraConfig
PeftModel
get_peft_model
add_adapter
set_adapter
active_adapter
adapter
task_module
task_modules
module_list
compose
composition
weighted
weight
matching_weight
routing
router
task_key
task_vector
task_embedding
meta_embedding
metadata
beta
temperature
scale
q_proj
v_proj
lora_A
lora_B
lora_alpha
lora_dropout
requires_grad
detach
no_grad
freeze
named_parameters
optimizer
parameter_group
state_dict
forward
inference
generate
```

重点检查但不限于：

```text
src/run_continual_causal_llama2.py
tasks/mtl5/dataloader_mtl_causal_llama.py
run_continual_script/keshi_llama/scrip.sh
模型定义文件
自定义 PEFT/LoRA 文件
训练器文件
推理脚本
checkpoint 加载文件
metadata encoder / key encoder 文件
```

请从训练入口一路追踪到：

```text
模型初始化
LoRA adapter 创建
任务模块注册
路由权重计算
组合模块生成
模型 forward
损失计算
反向传播
checkpoint 保存
推理加载与 generate
```

## 三、必须核查的具体事项

### 1. 任务模块的真实类型

明确论文中的任务模块 (P_k) 在代码中究竟对应什么：

* 一个完整的 LoRA adapter；
* LoRA 的 (A_k) 矩阵；
* LoRA 的 (B_k) 矩阵；
* 完整低秩更新 (B_kA_k)；
* LoRA adapter 的输出；
* adapter 对隐藏状态产生的增量；
* PEFT 中的 adapter 名称或状态；
* 其他自定义表示。

请给出：

* 对应的类名；
* 对应的变量名；
* 定义文件和准确行号；
* 调用文件和准确行号；
* 论文符号 (P_k) 与代码对象之间的映射。

### 2. 加权组合到底发生在哪一层

必须明确判断加权组合属于以下哪一种，并提供代码证据。

#### 情况 A：分别组合 LoRA 参数

[
A_{\mathrm{comp}}=\sum_k w_kA_k,
\qquad
B_{\mathrm{comp}}=\sum_k w_kB_k.
]

#### 情况 B：组合完整的 LoRA 权重增量

[
\Delta W_{\mathrm{comp}}
========================

\sum_k w_k\frac{\alpha}{r}B_kA_k.
]

#### 情况 C：组合各适配器的输出

[
\Delta h_{\mathrm{comp}}
========================

\sum_k w_k
\left(
\frac{\alpha}{r}B_kA_kh
\right).
]

#### 情况 D：组合 adapter 参数或输出的其他实现

请特别检查：

* 权重 (w_k) 在矩阵乘法之前还是之后使用；
* 权重是乘在 (A_k)、(B_k)、(B_kA_k) 还是 adapter output 上；
* 是否存在分别加权 (A) 和 (B) 导致交叉项的情况；
* 是否在每个 Transformer 层分别组合；
* 所有目标层是否共享同一组路由权重；
* 权重是否按样本、按 batch、按层或按 token 变化；
* 权重的 shape 以及广播方式；
* 是否对路由权重使用 softmax、sigmoid、temperature 或归一化。

请依据代码写出“单个目标线性层”的真实前向计算公式。例如：

[
h'
==

W_0h+
\sum_k w_k(x)\frac{\alpha}{r}B_kA_kh.
]

公式必须与实际代码完全一致。

### 3. LoRA 的完整配置

请核查并报告：

* rank (r)；
* `lora_alpha`；
* 实际缩放系数；
* 缩放公式是 (\alpha/r)、(\alpha/\sqrt r) 还是其他形式；
* `lora_dropout`；
* target modules；
* 是否只作用于 `q_proj` 和 `v_proj`；
* 是否还作用于 `k_proj`、`o_proj`、MLP 或其他层；
* bias 设置；
* LoRA 初始化方式；
* 是否使用 4-bit 或 8-bit 量化；
* 是否使用 QLoRA；
* 是否启用 `use_rslora`、`fan_in_fan_out`、`modules_to_save` 等选项；
* 各任务 adapter 是否具有相同的 rank、alpha、dropout、目标层和参数规模；
* 是否存在某个任务使用不同配置的情况。

请分别给出：

* 配置来源；
* 命令行参数；
* 默认值；
* 实际运行脚本覆盖后的最终值；
* 文件和准确行号。

### 4. 当前任务模块是否参与式（6）的组合

假设当前正在训练第 (n) 个任务，请准确确定组合集合是：

[
{1,\ldots,n-1},
]

还是：

[
{1,\ldots,n},
]

或其他集合。

必须回答：

1. 式（6）对应的代码循环范围是什么；
2. 训练第一个任务时如何处理没有旧模块的情况；
3. 当前任务模块 (P_n) 是否参与路由权重计算；
4. 当前任务模块是否参与加权组合；
5. 当前模块是否以未加权的形式单独加入模型；
6. 如果旧模块组合和当前模块同时存在，它们是相加、串联、激活切换还是其他关系；
7. 当前模块是否只用于学习新任务，而组合模块仅提供旧知识；
8. 训练完成后，当前模块何时转为冻结旧模块；
9. 代码中的任务索引是否从 0 或 1 开始；
10. 论文中的 (K)、(n)、(P_k) 与代码索引如何对应。

请写出训练任务 (n) 时的真实公式。

### 5. 训练阶段的完整前向过程

请按实际执行顺序描述：

1. 输入文本如何编码为 query representation；
2. metadata embedding 如何载入；
3. trainable task vector 如何初始化；
4. (\beta_k) 如何计算；
5. dynamic task representation 如何计算；
6. routing/matching weights 如何计算；
7. 哪些 LoRA 模块被激活或调用；
8. 旧模块组合如何进入 Transformer；
9. 当前任务模块如何进入 Transformer；
10. 最终输出和损失如何计算；
11. `matching loss`、`orthogonal loss` 和 `L2 loss` 分别作用于哪些参数；
12. 代码中是否存在论文未描述的额外损失。

请提供简洁伪代码，例如：

```python
query = encode(x)
weights = router(query, metadata, task_vectors)
old_output = weighted_old_adapters(x, weights)
current_output = current_adapter(x)
output = base_output + old_output + current_output
loss = lm_loss + lambda1 * ortho_loss + lambda2 * norm_loss
```

伪代码必须与真实实现一致。

### 6. 推理阶段的完整过程

请单独检查推理代码，不要直接假设训练和推理一致。

必须确认：

* 推理时是否加载全部已学习任务 adapter；
* 是否加载最终任务的 adapter；
* 推理时是否存在“当前任务模块”的概念；
* 是否对全部任务模块进行路由；
* 是否只对旧模块进行组合；
* 是否先预测任务，再选择 adapter；
* 是否执行 soft weighted composition；
* 是否使用一个固定 adapter；
* 是否根据输入动态计算权重；
* 推理时 metadata embedding 和 task vector 是否固定；
* 推理阶段是否使用与训练相同的 temperature 和归一化方式；
* 是否存在训练/推理实现不一致；
* 生成阶段是否会关闭某些 adapter 或 loss-related components。

请写出推理阶段真实公式和伪代码。

### 7. 梯度流与冻结状态

请建立完整参数表，至少覆盖以下对象：

| 参数类别                               | 训练第 (n) 个任务时是否参与前向 | requires_grad | 是否进入 optimizer | 是否接收梯度 | 是否更新 |
| ---------------------------------- | -----------------: | ------------: | -------------: | -----: | ---: |
| Base LLM parameters                |                    |               |                |        |      |
| Current task LoRA (P_n)            |                    |               |                |        |      |
| Old task LoRA (P_1,\ldots,P_{n-1}) |                    |               |                |        |      |
| Current task vector                |                    |               |                |        |      |
| Old task vectors                   |                    |               |                |        |      |
| Static metadata embeddings         |                    |               |                |        |      |
| Metadata encoder                   |                    |               |                |        |      |
| Query encoder                      |                    |               |                |        |      |
| Router/matching parameters         |                    |               |                |        |      |
| Temperature/scale parameter        |                    |               |                |        |      |
| Dynamic fusion parameters          |                    |               |                |        |      |

需要特别核查：

* 旧 LoRA adapter 是否设置 `requires_grad=False`；
* 是否仅仅没有放入 optimizer，而没有显式冻结；
* 旧 adapter 虽不更新，梯度是否仍穿过其输出流向路由权重；
* 组合权重是否可微；
* 旧 adapter output 是否被 `detach()`；
* old task vectors 是否被冻结；
* metadata embeddings 是 tensor、buffer 还是 Parameter；
* metadata embeddings 是否被 `detach()`；
* metadata encoder 是否预编码后完全移除；
* 路由参数是否随每个新任务持续更新；
* 当前任务训练会不会改变旧任务的路由表示；
* orthogonal loss 是否会向旧 task vectors 传播梯度；
* L2 loss 是否作用于全部 task vectors 或仅当前 task vector；
* 基础模型在 4-bit 情况下是否完全冻结；
* 是否存在意外可训练参数。

请检查：

```python
requires_grad
named_parameters()
optimizer.param_groups
detach()
torch.no_grad()
register_buffer()
nn.Parameter
freeze
```

如项目已有训练日志中的 trainable parameter 数量，请用它验证代码分析结果。

### 8. 路由权重和模块组合的关系

请明确：

* (W_k) 是否直接控制 LoRA contribution；
* (W_k) 是否经过截断、top-k、mask 或 threshold；
* 当前任务是否有固定额外权重；
* 权重和是否为 1；
* 是否包含 residual/base model contribution；
* 是否存在 adapter scaling 与 routing weight 的双重缩放；
* 最终某个任务模块的有效系数是否为：

[
w_k\frac{\alpha}{r};
]

* batch 内不同样本是否具有不同的 adapter 权重；
* 推理生成多个 token 时，路由权重是只计算一次还是每个 token 重新计算。

### 9. checkpoint 中实际保存了什么

请检查最终 `state_dict.pt`、adapter 目录和保存代码，明确：

* 是否保存所有任务的 LoRA (A/B)；
* 是否保存 task vectors；
* 是否保存 metadata embeddings；
* 是否保存 router parameters；
* 是否保存 base model；
* 每个 task adapter 的命名规则；
* checkpoint 恢复时如何重建所有 adapter；
* 推理时如何确认 adapter 顺序与 task vector 顺序一致；
* checkpoint 是否包含 optimizer state；
* final checkpoint 是否对应学习完第六个任务后的状态。

不要复制或展开大模型权重，只读取 key 名称、shape 和必要元数据。

### 10. 各任务模块是否结构完全一致

请逐任务比较：

* adapter 名称；
* 参数 key；
* 参数 shape；
* rank；
* target layers；
* trainable parameter count；
* LoRA alpha；
* dropout；
* scaling；
* 是否缺失某些层。

输出对比结论：

```text
All task-specific LoRA modules have identical architectures: Yes / No / TBD
```

若为 Yes，请给出代码及 checkpoint key/shape 证据。
若为 No，请列出差异。

### 11. 论文式（6）与代码的一致性

论文当前式（6）为：

[
P'*n=\sum*{k=1}^{K}W_kP_k.
]

请判断：

* 完全正确；
* 求和范围错误；
* 符号定义不完整；
* 将参数组合错误地写成模块组合；
* 未表示当前模块的单独作用；
* 训练和推理应使用不同公式；
* 需要补充层索引；
* 需要补充 LoRA scaling；
* 需要改写为 adapter output composition。

请给出“最小修改原则”下推荐的公式，但必须严格对应代码，不要提出代码中不存在的新机制。

### 12. 是否需要重新实验

根据核查结果，将情况归入以下一类：

#### A. 仅论文描述不清，代码和原实验机制正确

说明：

```text
No new experiment is required. Only the formulation, implementation details, training/inference descriptions, and gradient-flow statements need clarification.
```

#### B. 论文公式与代码不一致，但可以通过修改论文准确描述代码

说明是否仍不需要重跑实验，并指出应如何修正论文。

#### C. 代码实现与作者声称的机制实质不一致

指出：

* 不一致发生在哪里；
* 是否会影响方法定义；
* 是否影响已报告结果的解释；
* 是否必须修改代码；
* 修改代码后是否需要重新训练和重新评估。

不要为了迎合预期而选择 A，必须以事实为准。

## 四、输出文件结构

请将 `ADAPTER_COMPOSITION_AUDIT.md` 写成以下结构：

```markdown
# Adapter Composition Implementation Audit

## 1. Executive Conclusion
- Actual module type:
- Actual composition level:
- Modules included during training:
- Modules included during inference:
- Current-task module role:
- Whether old modules are frozen:
- Whether old task vectors are frozen:
- Whether metadata embeddings are trainable:
- Whether routing parameters are trainable:
- Need for new experiments: Yes / No / Conditional

## 2. Code Entry Points
| Functionality | File | Class/Function | Lines | Description |

## 3. Definition of a Task-Specific Module
- Paper symbol:
- Code object:
- Module type:
- Parameter structure:
- Evidence:

## 4. LoRA Configuration
| Item | Actual Value | Source File and Lines | Notes |
至少包含 rank、alpha、effective scaling、dropout、target modules、bias、quantization、adapter architecture consistency。

## 5. Exact Adapter Composition Mechanism
- Composition category:
- Weight shape:
- Composition location:
- Layer-wise behavior:
- Exact mathematical formula:
- Exact code evidence:

## 6. Training-Time Behavior
### 6.1 Modules included
### 6.2 Role of old composed modules
### 6.3 Role of the current module
### 6.4 First-task special case
### 6.5 Training formula
### 6.6 Training pseudocode

## 7. Inference-Time Behavior
### 7.1 Loaded modules
### 7.2 Routing process
### 7.3 Inference formula
### 7.4 Inference pseudocode
### 7.5 Differences from training

## 8. Gradient-Flow Audit
使用完整参数表，并提供 requires_grad、optimizer、detach/no_grad 等代码证据。

## 9. Routing and Metadata Parameters
- Static or trainable:
- Updated across tasks or frozen:
- Gradient sources:
- Relevant losses:
- Code evidence:

## 10. Checkpoint Contents
| Artifact | Included Parameters | Key Examples | Shapes | Loading Code |

## 11. Architecture Consistency Across Task Modules
| Task Adapter | Rank | Target Layers | Parameter Shapes | Parameter Count | Identical |
- Final conclusion:

## 12. Comparison Between Equation (6) and Implementation
- Current manuscript meaning:
- Actual implementation:
- Identified ambiguity or mismatch:
- Minimal correction required:

## 13. Manuscript-Ready Technical Facts
提供一段简洁英文事实说明，准确覆盖：
- module type；
- composition object；
- LoRA rank、target projections、scaling 和 dropout；
- identical architectures；
- old/current module roles；
- training/inference difference；
- gradient flow。

不要写完整审稿回复，只提供可用于后续撰写回复的技术事实。

## 14. Need for Additional Experiments
- Verdict:
- Reason:
- Whether retraining is necessary:
- Whether existing results remain valid:

## 15. Unconfirmed Items
所有无法确认的问题统一列为 TBD。

## 16. Evidence Appendix
逐项列出：
- File path
- Line range
- Minimal relevant code snippet
- Interpretation
```

## 五、证据要求

每个关键结论都必须提供：

```text
文件路径
类或函数名称
准确行号范围
最小必要代码片段
对代码含义的解释
```

不要只写“在某文件中发现”。例如：

```text
File: xxx/modeling_xxx.py
Function: forward
Lines: 210–228
Evidence: ...
Conclusion: Routing weights are applied to individual adapter outputs rather than to LoRA A/B matrices.
```

## 六、可使用的检查命令

```bash
rg -n "lora_A|lora_B|LoraConfig|lora_alpha|lora_dropout|target_modules" .
rg -n "add_adapter|set_adapter|active_adapter|adapter_name|task_module" .
rg -n "matching_weight|routing|router|task_vector|task_embedding|meta_embedding" .
rg -n "requires_grad|detach|no_grad|optimizer|param_groups|named_parameters" .
rg -n "state_dict|save_pretrained|load_state_dict|checkpoint" .
rg -n "forward|generate|inference" src tasks test_infer test_infer_RAG .
```

可选地使用 Python 检查 checkpoint key 和 shape，但不要打印完整权重：

```python
import torch

state = torch.load("PATH_TO_STATE_DICT", map_location="cpu")
obj = state.get("state_dict", state) if isinstance(state, dict) else state

for key, value in obj.items():
    if any(term in key.lower() for term in [
        "lora", "adapter", "task", "key", "router", "meta"
    ]):
        shape = tuple(value.shape) if hasattr(value, "shape") else type(value).__name__
        print(key, shape)
```

如需检查模型参数状态，只打印：

```text
parameter name
shape
requires_grad
是否进入 optimizer
```

不要打印参数值。

## 七、最终要求

* 不得编造实现细节；
* 不得用论文描述代替代码证据；
* 必须区分训练和推理；
* 必须明确当前任务模块是否参与式（6）；
* 必须明确组合的是 LoRA 参数、权重增量还是 adapter output；
* 必须明确所有关键参数的梯度流；
* 必须判断是否需要重新实验；
* 报告应足够完整，使未阅读代码的人也能据此准确修改论文公式和撰写审稿回复。
